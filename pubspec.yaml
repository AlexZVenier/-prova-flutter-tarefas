name: prova_mobile
description: Um mini cadastro de tarefas profissionais. # Adicione uma descrição
# O resto do seu arquivo pubspec.yaml continua abaixo
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0 
  path_provider: ^2.1.1 
  path: ^1.8.3
  # ...outras dependências

dev_dependencies:
  flutter_test:
    sdk: flutter
